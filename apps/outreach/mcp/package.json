{
  "name": "outreach-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "outreach-mcp": "dist/server.js"
  },
  "scripts": {
    "build": "tsc",
    "bundle": "tsc && esbuild dist/server.js --bundle --platform=node --format=esm --outfile=dist/server.bundle.js",
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.28.1",
    "typescript": "^5.6.0"
  }
}
